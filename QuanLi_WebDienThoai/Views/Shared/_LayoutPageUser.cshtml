<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng nhập - KookaburraKlub </title>

    <!-- Liên kết Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        /* Thiết lập nền */
        body {
            width: 100vw;
            height: 120vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: url('/Content/themes/images/img/bg.jpg') no-repeat center center fixed; /* Thay thế bằng đường dẫn hình nền của bạn */
            background-size: cover;
            position: relative;
        }

        a {
            text-decoration: none;
        }

        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.6); /* Màu đen mờ */
            z-index: 0;
        }

        /* Container của form đăng nhập */
        .login-container {
          
            position: relative;
            z-index: 1;
            width: 450px;
            max-width: 500px;
            padding: 50px 20px;
            background-color: #fff;
            border-radius: 20px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
            text-align: center;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }

        .login-container h2 {
            font-weight: bold;
            color: #333;
            margin-bottom: 10px;
            font-size: 30px;
        }

        .login-container p {
            color: #666;
            font-size: 0.9em;
            margin-bottom: 20px;
        }

        /* Tùy chỉnh nút */
        .btn-custom {
            width: 100%;
            background-color: #33aaff;
            color: white;
            font-weight: bold;
            padding: 10px;
        }

        .btn-custom:hover {
            background-color: #1a8cd8;
        }

        /* Đường dẫn dưới form */
        .login-container a {
            color: #33aaff;
            text-decoration: none;
            font-size: 0.9em;
           
        }

        .login-container a:hover {
            text-decoration: underline;
        }

        .form-group {
            text-align: left;
            width: 80%;
            margin: 0 auto;
        }
     
    </style>
</head>
<body>
    <div class="overlay"></div>
    <div>
        @RenderBody()
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>
